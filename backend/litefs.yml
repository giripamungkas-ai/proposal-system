mount-dir: /litefs
data-dir: ./database
exec: uvicorn app:app --host 0.0.0.0 --port 8000
fuse:
  allow-other: true
  dir: /litefs
lease:
  type: static
  candidate: true
  advertise-url: http://localhost:8080
